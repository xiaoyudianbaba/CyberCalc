/// 赛博朋克主题配色方案
/// 定义所有霓虹灯效颜色
import 'package:flutter/material.dart';

class CyberColors {
  CyberColors._();

  // ========== 背景色 ==========
  static const Color background = Color(0xFF0A0A0F);
  static const Color surface = Color(0xFF12121A);
  static const Color surfaceLight = Color(0xFF1A1A2E);

  // ========== 数字键 ==========
  static const Color numberButton = Color(0xFF1A1A2E);
  static const Color numberText = Color(0xFF00F0FF);
  static const Color numberGlow = Color(0x6600F0FF);

  // ========== 加法（霓虹紫） ==========
  static const Color addText = Color(0xFFB000FF);
  static const Color addGlow = Color(0x80B000FF);

  // ========== 减法（金色） ==========
  static const Color subtractText = Color(0xFFFFD700);
  static const Color subtractGlow = Color(0x80FFD700);

  // ========== 乘法（霓虹粉） ==========
  static const Color multiplyText = Color(0xFFFF006E);
  static const Color multiplyGlow = Color(0x80FF006E);

  // ========== 除法（霓虹绿） ==========
  static const Color divideText = Color(0xFF00FF9F);
  static const Color divideGlow = Color(0x8000FF9F);

  // ========== 等号 ==========
  static const Color equalsBackground = Color(0xFFFF006E);
  static const Color equalsText = Color(0xFFFFFFFF);
  static const Color equalsGlow = Color(0xCCFF006E);

  // ========== 功能键 ==========
  static const Color functionBackground = Color(0xFF2A2A3E);
  static const Color functionText = Color(0xFF8888AA);
  static const Color functionGlow = Color(0x4D4444AA);

  // ========== 显示区 ==========
  static const Color displayBackground = Color(0xFF050510);
  static const Color expressionText = Color(0xFF666688);
  static const Color resultText = Color(0xFFFFFFFF);

  // ========== 语音 ==========
  static const Color voiceActive = Color(0xFFFF0044);
  static const Color voiceGlow = Color(0x80FF0044);
  static const Color voiceIdle = Color(0xFF00FF9F);

  // ========== 状态 ==========
  static const Color success = Color(0xFF00FF9F);
  static const Color error = Color(0xFFFF0044);
  static const Color warning = Color(0xFFFFD700);
  static const Color info = Color(0xFF00F0FF);

  // ========== 文本 ==========
  static const Color primaryText = Color(0xFFFFFFFF);
  static const Color secondaryText = Color(0xFF8888AA);
  static const Color disabledText = Color(0xFF444466);

  // ========== 导航 ==========
  static const Color navBackground = Color(0xFF0D0D16);
  static const Color navActive = Color(0xFF00F0FF);
  static const Color navInactive = Color(0xFF444466);

  // ========== 历史 ==========
  static const Color historyBackground = Color(0xFF0A0A14);
  static const Color historyItem = Color(0xFF14142A);
  static const Color historyDivider = Color(0xFF1A1A3E);
}